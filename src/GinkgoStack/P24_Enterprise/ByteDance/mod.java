package GinkgoStack.P24_Enterprise.ByteDance;

/**
 * 一个数组中的n个数，任意选择k个，和为sum,求全局最大的sum % m;
 * 我觉得是是一个DP背包，当时只返回了一个dp[m-1]，通过10%
 * 应该从dp[m-1]开始，向后遍历dp,计算出最大的sum % m;
 *
 * 时间复杂度为O（n^2）
 */
public class mod {
}
